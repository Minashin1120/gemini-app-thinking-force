/**
 * Isolated-world content script.
 * Currently a light coordinator / future options bridge.
 * Main logic runs in page-hook.js (MAIN world) for fetch hooking.
 */
(function () {
  "use strict";
  // Reserved for options page messaging / badge updates if needed later.
  console.debug("[gemini-thinking-auto] content script loaded");
})();
